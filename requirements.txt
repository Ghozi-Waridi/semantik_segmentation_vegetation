numpy>=1.24
matplotlib>=3.7
Pillow>=9.0
tqdm>=4.66
# Optional GPU backends (pick one that suits your hardware):
# NVIDIA GPU (install the variant matching your CUDA, e.g. 12.x):
# cupy-cuda12x>=13.0
# Intel GPU via oneAPI:
# dpnp>=0.14
